export function analyzeTape(tape, submittedCode) { const firstCorrectPosition = submittedCode ? tape.indexOf(submittedCode) : null; return { totalCharsWritten: tape.length, finalCodeLength: submittedCode.length, rewriteRatio: submittedCode.length ? tape.length / submittedCode.length : 0, wasCloseEarly: firstCorrectPosition !== -1 && firstCorrectPosition < tape.length * 0.4, firstCorrectPosition: firstCorrectPosition === -1 ? null : firstCorrectPosition }; }


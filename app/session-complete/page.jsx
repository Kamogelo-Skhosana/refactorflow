"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
export default function SessionCompletePage() { const [session, setSession] = useState(null); useEffect(() => { const slug = new URLSearchParams(window.location.search).get("challenge"); if (slug) { const raw = window.localStorage.getItem(`refactorflow-session-${slug}`); if (raw) setSession(JSON.parse(raw)); } }, []); if (!session) return <main className="session-complete"><Link className="rf-back" href="/dashboard"><span>r</span><span>f</span></Link><section className="session-summary shell"><p className="kicker">Session complete</p><h1>Your session was saved.</h1><Link className="primary-button" href="/challenge">Choose another challenge <span>&rarr;</span></Link></section></main>; const analysis = session.tapeAnalysis || {}; return <main className="session-complete"><Link className="rf-back" href="/dashboard"><span>r</span><span>f</span></Link><section className="session-summary shell"><p className="kicker">Session complete</p><h1>Good work. Now<br /><em>notice the process.</em></h1><p className="session-intro">Your behavioral trail has been saved. This is the beginning of your RefactorFlow feedback loop.</p><div className="session-metrics"><article><span className="kicker">Time spent</span><strong>{Math.floor((session.durationSeconds || 0) / 60)}:{String((session.durationSeconds || 0) % 60).padStart(2, "0")}</strong></article><article><span className="kicker">Characters written</span><strong>{session.keystrokeCount || 0}</strong></article><article><span className="kicker">Backspaces</span><strong>{session.backspaceCount || 0}</strong></article><article><span className="kicker">Thrashing index</span><strong>{session.thrashingIndex ?? "â€”"}</strong><small>{session.classification || "Pending analysis"}</small></article></div><div className="session-insight"><p className="kicker">First reflection</p><h2>{analysis.wasCloseEarly ? "You found a promising direction early." : "Your solution evolved through exploration."}</h2><p>{analysis.rewriteRatio ? `Your rewrite ratio was ${analysis.rewriteRatio.toFixed(1)}Ã—. A detailed report will turn this signal into coaching.` : "Your detailed coaching report will be available after analysis."}</p></div><div className="session-actions"><Link className="primary-button" href="/challenge">Try another challenge <span>&rarr;</span></Link><Link className="secondary-button" href="/dashboard">Back to dashboard</Link></div></section></main>; }

